[project]
name = "wellai-bot-proto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["grpcio-tools>=1.80.0", "mypy-protobuf>=5.0.0"]

[tool.setuptools.packages.find]
where = ["gen/py"]
namespaces = true
